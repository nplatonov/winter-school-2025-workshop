---
output: xaringan::moon_reader
toc: true
toc_depth: 4
---

### QGIS {#soft}

#### Standalone {#qgis-standalone}

<!-- [QGIS Web](https://qgis.org/en/site/) -->
Сайт https://qgis.org/. 
Находим "Download"

<iframe src="https://qgis.org/" width=100% height=598>
</iframe>


---

name: soft-qgis-standalone-next

https://qgis.org/en/site/forusers/download.html

<iframe src="https://qgis.org/en/site/forusers/download.html" width=100% height=670>
</iframe>

#### OSGeo4W {#soft-qgis-osgeo4w}

Программное обеспечение с открытым исходным кодом для Windows https://trac.osgeo.org/osgeo4w/

<iframe src="https://trac.osgeo.org/osgeo4w/#QuickStartforOSGeo4WUsers" width=100% height=555>
</iframe>

Находим "Download the ​OSGeo4W network installer"

---

```{r osgeo4w, out.height=670, out.extra="contain"}
knitr::include_graphics("assets/OSGeo4w-setup.png")
```

#### NextGIS QGIS {#soft-nextgis}

https://nextgis.com/nextgis-qgis/ (Windows)

<iframe src="https://nextgis.com/nextgis-qgis/" width=100% height=530>
</iframe>

Находим "Download NextGIS QGIS"

```{r nextslide1, eval=grepl("moon_reader",names(rmarkdown::metadata$output)), results='asis'}
cat("\n---\nclass: middle\n\n")
```

#### Расширения {#soft-qgis-plugins}

https://plugins.qgis.org/

```{r}
knitr::include_url("https://plugins.qgis.org/plugins/?sort=-downloads",height=630)
```

<!--
#### Good practice

Используем поисковые системы, ссылки могут устареть.
-->

### R

https://www.r-project.org/.
Выбираем "download R" и сервер для скачивания (mirror).

```{r}
knitr::include_url("https://www.r-project.org/",height=630)
```

#### Ядро

https://cloud.r-project.org/

```{r}
knitr::include_url("https://cloud.r-project.org/banner.shtml",height=630)
```

#### Пакеты

https://cloud.r-project.org/web/packages/

```{r}
knitr::include_url("https://cloud.r-project.org/web/packages/",height=630)
```

---

[Отображение всех пакетов](https://cloud.r-project.org/web/packages/available_packages_by_name.html)

```{r}
knitr::include_url("https://cloud.r-project.org/web/packages/available_packages_by_name.html",height=630)
```

---

```{r, echo=FALSE, eval=!ursa:::.isRemark()}
if (isNamespaceLoaded("ursa"))
   unloadNamespace("ursa")
if (!dir.exists("Rlibs"))
   dir.create("Rlibs")
.libPaths(c("Rlibs",.libPaths())[1])
```

```{r, echo=TRUE, eval=TRUE}
pkgList <- c("png","mapview","adehabitatLT","readxl","tidyr","lwgeom"
            ,"geosphere","ggstatsplot","ursa")
repos <- "https://cloud.r-project.org/"
type <- if (.Platform$OS.type=="windows") "binary" else getOption("pkgType")
```

```{r, echo=TRUE, eval=FALSE}
available <- sapply(pkgList,function(pkg) {
   if (requireNamespace(pkg))
      return(TRUE)
   install.packages(pkg,repos=repos,type=type)
   requireNamespace(pkg)
})
```

```{r, echo=FALSE, eval=!FALSE}
available <- sapply(pkgList,function(pkg) {
   if (requireNamespace(pkg))
      return(TRUE)
   install.packages(pkg,contriburl="file:///D:/Product.ins/packages/zip4")
   requireNamespace(pkg)
})
```

```{r, echo=TRUE, eval=FALSE}
if (packageVersion("ursa")<"3.11.2")
   install.packages("ursa",repos=repos,type=type)
```

```{r, echo=FALSE, eval=!ursa:::.isRemark()}
if (packageVersion("ursa")<"3.11.2") {
   unloadNamespace("ursa")   
   install.packages("C:/platt/R/ursa-package/ursa_3.11.2-1158.zip")
}
```

```{r, echo=TRUE, eval=TRUE}
packageVersion("ursa")
available
c('Everything is ok?'=all(available))
```
